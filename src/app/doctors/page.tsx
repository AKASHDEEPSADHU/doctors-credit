export const metadata = { title: "Doctors — Doctor's Credit" };

export default function DoctorsPage() {
  return (
    <main id="main" className="legal">
      <p className="eyebrow">Meet the specialists</p>
      <h1>No invented names.</h1>
      <p>
        Specialist profiles will include name, specialty, hospital, qualifications,
        registration information where appropriate, languages and an official
        profile link — only when those facts can be sourced. Until then, this
        page stays empty on purpose.
      </p>
      <p>DCredit does not independently diagnose or prescribe.</p>
    </main>
  );
}
